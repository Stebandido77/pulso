"""Placeholder; the metadata smoke output now lives in the Phase-2 report.

Kept as a stub because the orchestration environment cannot delete files
during this session. Not collected by pytest (no ``test_*`` functions).
"""
